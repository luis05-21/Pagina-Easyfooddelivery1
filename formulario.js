document.getElementById("formularioparapedidos").addEventListener("submit", function(event) {
    event.preventDefault(); // Previene el envío del formulario y recarga de la página
    alert("Su Pedido fue Recibido");
});
